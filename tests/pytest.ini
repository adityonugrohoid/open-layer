[pytest]
asyncio_mode = auto
markers =
    level1: Level 1 (Core) conformance tests
    level2: Level 2 (Thinking) conformance tests
    thinking: Tests requiring thinking/reasoning models
testpaths = suite
addopts = -v --tb=short
